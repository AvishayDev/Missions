import { StyleSheet } from "react-native";

export const missionsStyles = StyleSheet.create({
  missionsList: {
    height: "100%",
  },
  pageTitle: {
    fontSize: 50,
    fontWeight: "bold",
  },
  headerContainer: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
    marginTop: 20,
  },
});
