import { StyleSheet } from "react-native";

export const missionsStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  missionContainer: {
    margin: 10,
    padding: 10,
    flex: 1,
  },
});
